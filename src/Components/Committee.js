// frontend/src/components/Committee.js
import React from 'react';
import { Typography, Container } from '@mui/material';

const Committee = () => (
  <Container>
    <Typography variant="h4" gutterBottom>Committee</Typography>
    <Typography variant="body1">Details about people working on the project...</Typography>
  </Container>
);

export default Committee;
