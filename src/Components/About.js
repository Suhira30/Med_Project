// frontend/src/components/About.js
import React from 'react';
import { Typography, Container } from '@mui/material';

const About = () => (
  <Container>
    <Typography variant="h4" gutterBottom>About the Project</Typography>
    <Typography variant="body1">Our vision is to...</Typography>
    <Typography variant="body2">Details about the project...</Typography>
  </Container>
);

export default About;
